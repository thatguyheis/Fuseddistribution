#!/bin/sh

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required to commit in this repository." >&2
  exit 1
fi

exec gitleaks git . --pre-commit --staged --timeout 120 --config .gitleaks.toml --redact --no-banner
